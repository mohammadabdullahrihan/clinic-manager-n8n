# ⚙️ টেকনিক্যাল সেটআপ ও ডেপ্লয়মেন্ট গাইড (Master Deployment Guide)
**Target:** Rayyan Quantum AI Labs Integration Specialists | **Version:** 2.5 (Enterprise)

এই গাইডটি অত্যন্ত জটিল কারিগরি ধাপগুলোকে অত্যন্ত সহজভাবে উপস্থাপন করেছে। সঠিক ইমপ্লিমেন্টেশনের জন্য প্রতিটি পয়েন্ট গুরুত্ব সহকারে দেখুন।

---

## ১. মেটা হোয়াটসঅ্যাপ ক্লাউড এপিআই সেটআপ (The Foundation)

১. **Meta Developer Account:** [developers.facebook.com](https://developers.facebook.com/) এ গিয়ে লগইন করুন।
২. **Create App:** "Other" -> "Business" সিলেক্ট করে অ্যাপ তৈরি করুন।
৩. **WhatsApp Menu:** ড্যাশবোর্ড থেকে বাম দিকে স্ক্রল করে WhatsApp প্রোডাক্টটি অ্যাড করুন।
৪. **Temporary vs Permanent Token:**
   - ডেভলপমেন্টের সময় টেম্পোরারি টোকেন ব্যবহার করুন।
   - প্রোডাকশন পিরিয়ডে **Business Settings > System Users** এ গিয়ে একটি পার্মানেন্ট টোকেন জেনারেট করুন। একে `Full Access` দিন এবং `whatsapp_business_messaging` পারমিশনটি অবশ্যই নিশ্চিত করুন।
৫. **Test Number:** মেটা থেকে দেয়া একটি টেস্ট নম্বর দিয়ে প্রথম ধাপের মেসেজিং ভেরিফাই করুন।

---

## ২. গুগল জেমিনি এআই ইন্টিগ্রেশন (LLM Configuration)

১. **Google AI Studio:** [aistudio.google.com](https://aistudio.google.com/) থেকে API Key জেনারেট করুন।
২. **Security Check:** এপিআই কি-টি এনক্রিপ্টেড রাখার চেষ্টা করুন এবং কোথাও পাবলিকলি শেয়ার করবেন না।
৩. **n8n Configuration:** `Google Gemini Chat Model` নোডে গিয়ে `Model Name` হিসেবে `gemini-1.5-flash` বা `gemini-1.5-pro` সিলেক্ট করুন। (Flash দ্রুত রেসপন্স দেয়, Pro বেশি বুদ্ধিমান)।

---

## ৩. গুগল শিট ডাটাবেজ স্ট্রাকচার (Database Normalization)

একটি গুগল শিট তৈরি করুন এবং নিচের ট্যাবগুলো হুবহু তৈরি করুন। কলামের স্পেলিং ভুল হলে ডাটা এন্ট্রি হবে না।

### Tab: `Patients`
| patient_id | whatsapp_number | name | age | gender | created_at |
| :--- | :--- | :--- | :--- | :--- | :--- |

### Tab: `Appointments`
| appointment_id | patient_id | whatsapp_number | date | time | status |
| :--- | :--- | :--- | :--- | :--- | :--- |

### Tab: `Config`
| key | value |
| :--- | :--- |
| working_hours | 09:00-18:00 |
| business_name | [Your Clinic Name] |

---

## ৪. n8n ওয়ার্কফ্লো ইমপ্লিমেন্টেশন (Logic Orchestration)

১. **Import:** n8n ড্যাশবোর্ডে গিয়ে `mega-clinic-manager.json` ফাইলটি ইমপোর্ট করুন।
২. **Credentials Setup:** n8n এর বাম পাশের মেনু থেকে Credentials এ গিয়ে নিচেরগুলো অ্যাড করুন:
   - `WhatsApp Business Cloud API`
   - `Google Gemini Chat Model API`
   - `Google Sheets API` (Service Account ব্যবহার করা সবচেয়ে ভালো)।
৩. **Service Account Email:** আপনার গুগল ক্লাউড সার্ভিস একাউন্টের ইমেলটি গুগল শিটে 'Editor' হিসেবে যোগ করুন।
৪. **Webhook:** `WhatsApp Trigger` নোড থেকে Production URL টি কপি করুন।

---

## ৫. হোয়াটসঅ্যাপ ওয়েবহুক অ্যাক্টিভেশন (Handshake Verification)

১. আপনার মেটা অ্যাপের ড্যাশবোর্ডে **Configuration** ট্যাবে যান।
২. n8n থেকে কপি করা URL টি হোয়াটসঅ্যাপের `Webhook URL` এর ঘরে বসান।
৩. একটি `Verify Token` দিন (যেমন: `RAYYAN_QUANTUM_2026`) এবং n8n এর ট্রিগারেও সেটি হুবহু দিন।
৪. **Fields:** `Webhooks` সেকশনে গিয়ে **Messages** অপশনটি অবশ্যই Subscribe করুন।

---

## ৬. প্রি-লঞ্চ টেস্ট চেকলিস্ট (Quality Assurance Checklist)

- [ ] হাই (Hi) লিখলে বট কি ক্লিনিকের নামে স্বাগতম জানাচ্ছে?
- [ ] ভুল তথ্য (যেমন- ভুল বয়স) দিলে কি এআই সেটি বুঝতে পারছে?
- [ ] অ্যাপয়েন্টমেন্ট বুকিং করলে কি গুগল শিটে রিয়েল-টাইমে ডাটা জমা হচ্ছে?
- [ ] একই সময় ও তারিখে দুইজন রুগী বুক করতে চাইলে সিস্টেমটি বাধা দিচ্ছে কি?
- [ ] সকাল ৮টার রিমাইন্ডার কি অটোমেটিক ট্রিগার হচ্ছে?

---

## 🛠 মেইনটেইন্যান্স প্রোটোকল (Maintenance)
- **Log Review:** প্রতি সপ্তাহে একবার n8n-এর `Execution History` চেক করুন কোনো এরর হয়েছে কিনা।
- **Token Expiry:** পার্মানেন্ট টোকেন থাকলেও মেটা অ্যাপের স্ট্যাটাস চেক করুন।

**Rayyan Quantum AI Labs Technical Team**
Email: tech-support@rayyanailabs.com

---
*আপনার নির্ভুল কনফিগারেশন একটি সফল ক্লিনিক ম্যানেজমেন্টের চাবিকাঠি।*
